class Issue {
    constructor(key, summary, typeIcon, url) {
        this.key = key
        this.summary = summary
        this.typeIcon = typeIcon
        this.url = url
    }
}

export default Issue
